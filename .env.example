# ========================================
# P2P FILE SHARE - ENVIRONMENT VARIABLES
# ========================================

# ----------------------------------------
# RELAY SERVER CONFIGURATION (cho Client)
# ----------------------------------------

# URL của relay server chung (để kết nối qua Internet)
# Nếu set → client sẽ kết nối relay server này
# Nếu không set → client sẽ tự khởi động local relay server
# RELAY_SERVER_URL=https://p2p-relay-server.onrender.com
# RELAY_SERVER_URL=http://localhost:8080

# Có tự động khởi động local relay server không? (true/false)
# - true: Tự start relay server local (mặc định)
# - false: Không start, chỉ kết nối server remote
# START_RELAY_SERVER=false

# ----------------------------------------
# STANDALONE RELAY SERVER (cho Deploy)
# ----------------------------------------

# Port server sẽ chạy (Render tự động set)
# PORT=10000

# Thư mục lưu trữ file relay
# STORAGE_DIR=/tmp/relay-storage

# Thời gian hết hạn file (giờ)
# FILE_EXPIRY_HOURS=24

# Kích thước file tối đa (MB)
# MAX_FILE_SIZE_MB=100

# Enable CORS (cho web clients)
# ENABLE_CORS=true

# JVM options (cho Render/cloud)
# JAVA_TOOL_OPTIONS=-Xmx512m -Xms256m

# ----------------------------------------
# EXAMPLES
# ----------------------------------------

# Example 1: Kết nối Render relay server
# RELAY_SERVER_URL=https://p2p-relay-server.onrender.com
# START_RELAY_SERVER=false

# Example 2: Kết nối local relay server khác
# RELAY_SERVER_URL=http://192.168.1.100:8080
# START_RELAY_SERVER=false

# Example 3: Tự động start local relay server (default)
# (Không cần set gì cả, hoặc:)
# START_RELAY_SERVER=true
